fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.28.1
beautifulsoup4==4.12.3
lxml==5.3.0
python-frontmatter==1.1.0
pdfplumber==0.11.4
anthropic==0.40.0
openai==1.109.1
google-genai==2.15.0
# google-genai(→google-auth)가 끌어오는 간접 의존성. cryptography 49부터 macOS x86_64(Intel)
# wheel 배포를 중단해서(arm64 전용), Intel Mac에서 uv/pip로 설치 시 Rust+Xcode Command Line
# Tools가 있어야만 소스 빌드가 되는 문제가 있었다 — 48.x는 universal2 wheel(Intel+Apple
# Silicon 겸용)이 있어 이 핀만으로 빌드 없이 설치된다. google-auth 2.56.2는 cryptography
# >=38.0.3만 요구해 48.x로도 충분하다(우분투 uv로 이 브랜치의 전체 requirements.txt를
# 교차 플랫폼 리졸브까지 실측 확인, 52개 패키지 충돌 없이 해석됨).
cryptography<49
pydantic==2.13.4
pydantic-settings==2.6.1
python-multipart==0.0.12
Pillow>=10.0.0
PyJWT>=2.8.0

# RAG(Agentic RAG) — PostgreSQL+pgvector 클라이언트. RAG는 opt-in 기능(RAG_POSTGRES_HOST 미설정 시
# 비활성)이지만, 패키지 자체는 항상 설치한다.
psycopg[binary]>=3.2.0
pgvector>=0.3.0
