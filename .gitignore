# 환경 변수 (API 키 포함)
.env

# 실험 스크립트·로그
scripts/
logs/

# Claude Code 로컬 설정 (훅에 봇 토큰 포함)
.claude/

# Codex 리뷰 문서 (의도적으로 git 미추적)
docs/review-w-codex/

# 실험 로그 (실제 지원 검토 회사명·평가 포함, 로컬 보관용 — git 히스토리에서도 제거함)
docs/phase*.md

# RAG 서브 프로젝트 계획 (Codex 작업 중, 로컬 보관용)
# prod worktree에는 dev worktree의 실제 디렉토리를 가리키는 심볼릭 링크로 존재함
# (2026-07-22 생성) — 끝에 슬래시 붙은 패턴은 실제 디렉토리만 매칭하고 심볼릭
# 링크는 매칭 안 하는 git 사양 때문에, 슬래시 없는 패턴을 하나 더 추가해야 함
docs/rag-project-plans/
docs/rag-project-plans

# 계획/설계 문서 (구현 전 아이디어·초안 — 공개 저장소에 로드맵 노출 원치 않음, 로컬 보관용)
docs/planning/

# RAG main 반영 작업 계획 (feat/rag-integration-plan 전용, 로컬 보관용 — 최종 main
# 반영 시 완성된 코드만 재작성하고 이 문서는 안 가져감)
docs/rag-integration/

# 기능 브랜치 전용 개발 문서(설계+히스토리) — 브랜치 하나로 바로 만드는 기능은
# docs/rag-integration/과 같은 패턴으로 docs/{기능명}/에 PLAN.md+HISTORY.md, 로컬 보관용
docs/profile-history/

# 데이터 (개인 정보 포함)
data/

# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
