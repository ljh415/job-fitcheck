# Claude → Codex 인계 워크플로우

Claude가 주 구현자이고 Codex는 계획·리뷰를 맡는다. Claude 사용량 제한이나 긴급 작업으로 Codex가 구현을 이어받을 때는 사용자가 별도로 설명한 내용과 함께 아래 순서로 현재 상태를 먼저 복원한다.

## 작업 시작

1. `CLAUDE.md`와 `docs/TODO.md`를 읽는다.
2. `git status --short --branch`, `git diff --stat`, `git diff`, `git diff --cached`, `git log -10 --oneline --decorate`로 브랜치·미커밋 변경·최근 작업을 확인한다.
3. 요청과 관련된 `README.md`, `CHANGELOG.md`, `docs/` 문서와 가장 최근의 `review*_codex*.md`만 추가로 읽는다.
4. 코드 변경 전 현재 기준점, 진행 중인 변경, 다음 할 일, 충돌 위험을 짧게 정리한다.

## 이어서 작업할 때

- 사용자가 전달한 Claude 인계 내용을 최우선으로 삼되 Git과 파일의 실제 상태로 검증한다.
- Claude 또는 사용자의 미커밋 변경은 보존하고, 관련 없는 파일은 수정하지 않는다.
- 요청이 계획·리뷰이면 파일을 변경하지 않는다. 구현 요청일 때만 최소 범위로 수정하고 관련 검증을 실행한다.
- `.env`, API 키, 개인 데이터와 `data/`의 실사용 자료를 출력하거나 커밋하지 않는다.
- 문서와 코드가 다르면 현재 코드와 테스트 결과를 기준으로 하고 차이를 알린다.

## 구현 책임

- 기본 구현자는 Claude다. Codex는 사용자나 Claude가 구현을 명시적으로 인계한 경우 실제 구현과 검증까지 맡는다.
- 같은 코드 영역은 한 시점에 한 에이전트만 수정한다. 구현자를 바꿀 때는 진행 상태와 미커밋 변경을 먼저 확인한다.
- 병렬 구현은 수정 파일과 책임 범위가 겹치지 않는 독립 작업에만 사용한다.
- 긴급 인계 시 Codex는 별도 재설계 없이 합의된 방향의 최소 변경으로 완료하고 Claude가 이어받을 수 있게 결과를 남긴다.

## 작업 종료

- 변경 파일, 검증 결과, 남은 작업을 짧게 남겨 Claude가 다시 이어받을 수 있게 한다.
- 커밋이나 푸시는 사용자가 요청한 경우에만 수행한다.
