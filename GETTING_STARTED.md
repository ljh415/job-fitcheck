# 처음 시작하기 (완전 초보자용 가이드)

프로그래밍을 몰라도 따라 할 수 있도록 순서대로 설명합니다. 총 15~20분 정도 걸립니다.

컴퓨터를 새로 사거나 재설정할 일은 없고, 아래 프로그램 2개만 새로 설치하면 됩니다: **Docker Desktop**, 그리고 (선택) **텍스트 편집기**.

---

## 1단계. Docker Desktop 설치

이 앱은 "Docker"라는 프로그램 위에서 실행됩니다. Docker는 컴퓨터 환경 차이(윈도우냐 맥이냐, 파이썬 버전이 뭐냐 등)를 신경 쓰지 않고 앱을 실행할 수 있게 해주는 도구입니다.

1. [https://www.docker.com/products/docker-desktop/](https://www.docker.com/products/docker-desktop/) 접속
2. 본인 컴퓨터에 맞는 버전 다운로드
   - Mac: "Mac with Apple Silicon"(M1/M2/M3/M4 칩) 또는 "Mac with Intel Chip" — Apple 메뉴 → 이 Mac에 관하여에서 확인 가능
   - Windows: "Windows" 버튼 클릭
3. 다운로드된 설치 파일 실행 → 안내대로 계속 진행 (Windows에서 "WSL2를 설치해야 합니다" 같은 안내가 나오면 그냥 안내를 따라 진행하고 재시작하면 됩니다)
4. 설치 후 Docker Desktop 실행 → 고래 모양 아이콘이 화면 상단(Mac) 또는 하단(Windows) 트레이에 뜨고, "Docker Desktop is running"이라고 나오면 준비 완료

> 이 아이콘이 안 떠 있으면(꺼져 있으면) 이후 단계가 전부 실패합니다. 실행 중인지 항상 먼저 확인하세요.

---

## 2단계. 프로젝트 다운로드

GitHub 저장소 페이지에서:

1. 초록색 **`Code`** 버튼 클릭
2. **`Download ZIP`** 클릭
3. 다운로드된 zip 파일의 압축을 풀기 (더블클릭하면 자동으로 풀리는 경우가 많습니다)
4. 압축 푼 폴더를 원하는 위치(예: 바탕화면)로 옮겨두기

> `git`을 이미 써보셨다면 `git clone` 명령어로 받으셔도 됩니다. 모르면 위 방법으로 충분합니다.

---

## 3단계. 터미널 열기

"터미널"은 글자로 명령을 입력해서 컴퓨터에 지시하는 창입니다. 아래 2번만 실행하면 되니 겁먹지 않으셔도 됩니다.

- **Mac**: `Command(⌘) + Space` → "터미널" 입력 → Enter
- **Windows**: 시작 메뉴 → "PowerShell" 검색 → Enter

터미널을 열었으면, 2단계에서 압축을 푼 폴더로 이동해야 합니다. 폴더 경로를 직접 타이핑할 필요 없이:

1. 터미널 창에 `cd ` 라고 입력(마지막에 띄어쓰기 한 칸)
2. 압축을 푼 폴더를 마우스로 끌어서 터미널 창 위에 놓기 → 경로가 자동으로 채워짐
3. Enter

터미널에 아래처럼 입력해서 폴더 안 파일 목록이 보이면 제대로 이동한 겁니다.

```bash
ls
```

(`docker-compose.yml`, `README.md` 같은 파일들이 보이면 정상입니다)

---

## 4단계. Claude API 키 발급받기

이 앱은 Anthropic(Claude)이라는 AI 회사의 API를 사용해서 채용공고를 분석합니다. API를 쓰려면 키(비밀번호 같은 문자열)가 필요하고, 사용한 만큼 비용이 청구됩니다 (공고 1건 분석에 대략 **$0.1 내외** — [README의 사용 비용 추정치](README.md#사용-비용-추정치) 참고).

1. [https://console.anthropic.com](https://console.anthropic.com) 접속 후 회원가입/로그인
2. 왼쪽 메뉴에서 **Billing**(결제) 들어가서 결제 수단(카드) 등록 및 최소 금액 충전 (보통 $5부터 가능)
3. 왼쪽 메뉴에서 **API Keys** 클릭 → **Create Key** 클릭
4. 생성된 키(`sk-ant-`로 시작하는 긴 문자열)를 복사 — **이 화면을 벗어나면 다시 볼 수 없으니** 메모장 등에 잠깐 붙여두세요

> Google Gemini는 무료로도 쓸 수 있지만(README의 "Gemini 무료 티어" 참고), 처음엔 Claude 키만 있어도 모든 기능이 정상 동작합니다. 나중에 추가해도 됩니다.

---

## 5단계. 설정 파일(.env) 만들기

터미널에 (3단계에서 이동한 폴더 안에서) 아래를 입력하고 Enter:

```bash
cp .env.example .env
```

이제 `.env`라는 설정 파일이 만들어졌습니다. 이 파일을 열어서 API 키를 넣어야 하는데, `.env`처럼 점(`.`)으로 시작하는 파일은 Finder/탐색기에 안 보이는 경우가 많아서, 터미널로 직접 여는 게 가장 확실합니다.

**Mac:**
```bash
open -e .env
```
(메모장 앱이 열립니다)

**Windows:**
```bash
notepad .env
```

열린 파일에서 아래 두 줄을 찾아 수정하세요.

```env
ANTHROPIC_API_KEY=sk-ant-여기에-4단계에서-복사한-키-붙여넣기
APP_SECRET=원하는-비밀번호-아무거나
```

- `ANTHROPIC_API_KEY`: 4단계에서 복사한 키를 `=` 뒤에 그대로 붙여넣기 (따옴표 없이, 앞뒤 공백 없이)
- `APP_SECRET`: 이 앱에 로그인할 때 쓸 비밀번호를 직접 정해서 입력 (예: `myapp1234`)

수정 후 저장(`Cmd+S` 또는 `Ctrl+S`)하고 파일을 닫으세요.

---

## 6단계. 앱 실행

터미널에서 (같은 폴더 안에서) 아래 명령을 입력하고 Enter:

```bash
docker compose up --build
```

처음 실행할 때는 필요한 파일들을 다운로드하느라 2~5분 정도 걸릴 수 있습니다. 화면에 로그가 계속 흘러가다가 멈추는 듯한 상태가 되면(예: `Uvicorn running on...` 같은 문구가 보이면) 준비된 겁니다. **이 터미널 창은 닫지 말고 그대로 켜두세요** — 창을 닫으면 앱도 같이 꺼집니다.

---

## 7단계. 접속 및 로그인

1. 브라우저(크롬 등)를 열고 주소창에 `http://localhost:8000` 입력
2. 5단계에서 정한 `APP_SECRET` 비밀번호 입력 → 로그인

---

## 8단계. 첫 사용

1. **설정** 탭 → 이력서/포트폴리오 PDF 업로드 → 프로필 자동 생성
2. **+ 회사 추가** → 채용공고 URL 붙여넣기 또는 텍스트/이미지 입력 → AI 분석
3. **대시보드**에서 결과 확인, Q&A로 질문도 가능

---

## 앱을 끄고 싶을 때

6단계에서 실행해둔 터미널 창에서 `Control + C`를 누르거나, 새 터미널 창에서 같은 폴더로 이동한 뒤 아래 입력:

```bash
docker compose down
```

다시 쓰고 싶으면 6단계(`docker compose up --build`)만 다시 실행하면 됩니다(설정 파일과 데이터는 그대로 남아있습니다).

---

## 문제가 생겼을 때

| 증상 | 원인/해결 |
|---|---|
| `docker: command not found` 또는 비슷한 오류 | Docker Desktop이 실행 중이 아닙니다. 아이콘을 찾아 실행한 뒤 다시 시도 |
| `port is already allocated` 같은 오류 | 8000번 포트를 다른 프로그램이 쓰고 있습니다. 컴퓨터를 재시작하거나, 다른 프로그램을 종료 후 재시도 |
| 로그인이 안 됨 | `.env` 파일의 `APP_SECRET` 값을 정확히 입력했는지 확인 (대소문자 구분) |
| 공고 분석 시 "인증" 관련 오류 | `.env`의 `ANTHROPIC_API_KEY`가 정확히 붙여넣어졌는지 확인 (앞뒤 공백·따옴표 없이) — 수정 후에는 6단계 명령을 다시 실행해야 반영됩니다 |
| 그 외 궁금한 점 | [README.md](README.md)에 기능별 설명이 더 자세히 있습니다 |
